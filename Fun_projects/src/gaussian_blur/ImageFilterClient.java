package gaussian_blur;

public class ImageFilterClient {
	public static void main(String[] args) {
        // Create a new File object with the image file name as argument (i.e. image.jpg)
        // Create a new LoadImage object with the loaded File as an argument
        // Get the BufferedImage from the LoadImage object and save it in a variable (originalImage)
        // This BufferedImage variable will represent the original, unchanged image
        // Create another LoadImage object with the same File as an argument
        // Get the BufferedImage from the LoadImage object and save it in another variable (resultImage)
        // This BufferedImage variable will be used with the GaussianBlur class
        // Using the resultImage, create a GaussianBlur object, call the applyFilter() method,
        // and store the result back into resultImage
        // Create the GUI components
}
}
